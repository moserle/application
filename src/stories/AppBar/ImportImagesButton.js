import React from 'react';
import { storiesOf } from '@storybook/react';
import ImportImagesButton from '../../components/AppBar/ImportImagesButton/ImportImagesButton';

storiesOf('ImportImagesButton', module).add('default', () => (
  <ImportImagesButton />
));
